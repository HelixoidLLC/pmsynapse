{
  "name": "pmsynapse",
  "version": "0.1.0",
  "private": true,
  "description": "AI-enabled end-to-end project management system",
  "repository": {
    "type": "git",
    "url": "https://github.com/HelixoidLLC/pmsynapse"
  },
  "license": "Apache-2.0",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "dev:desktop": "pnpm --filter @pmsynapse/desktop dev",
    "dev:vscode": "pnpm --filter pmsynapse dev",
    "dev:rpc": "pnpm --filter @pmsynapse/rpc dev",
    "lint": "turbo run lint",
    "lint:rust": "cargo fmt --check && cargo clippy --all-targets --all-features",
    "test": "turbo run test",
    "test:rust": "cargo nextest run --all-features",
    "typecheck": "turbo run typecheck",
    "clean": "turbo run clean && cargo clean",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "package:vscode": "pnpm --filter pmsynapse package"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "prettier": "^3.4.0",
    "turbo": "^2.3.0",
    "typescript": "^5.7.0"
  },
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  }
}
